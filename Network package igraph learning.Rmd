---
title: "Network using igrph"
author: "Daniel"
date: "19 de julio de 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r igraph}
require(igraph)


web <- read.csv("Data/WeddellSea_FW.csv", header = T, check.names = F)


str(web)

web <- web[ ,c(2,1)]    # TODAS LAS FILAS Y LAS COLUMNAS 2 Y 1
web

g <- graph_from_data_frame(web)

g

#E(g)  #the edges of the network
#V(g)  #the oodes of the network
#V(g)$name #the name of the nodes


deg <- degree(g, mode = "all")  #numero de todas las entradas y salidas a cada nodo
deg

deg <- degree(g, mode = "in")   #numero de flechas entrantes
deg

deg <- degree(g, mode = "out")   #numero de flechas salientes
deg





```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
#plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
